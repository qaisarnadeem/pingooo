<%= form_for(@prize_category) do |f| %>
  <% if @prize_category.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@prize_category.errors.count, "error") %> prohibited this prize_category from being saved:</h2>

      <ul>
      <% @prize_category.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>
  <div class="field">
    <%= f.label :description %><br>
    <%= f.text_field :description %>
  </div>
  <div class="field">
    <%= f.label :amount %><br>
    <%= f.number_field :amount %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
